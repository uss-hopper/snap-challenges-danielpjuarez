//loop will find all uniques factors of a given number and sum them.
// loop will subtract 1 from j each time it runs and divide 24 by the new value of j,
// and add that value, if it is an integer, to other values that are integers

var j;
foreach (j=24, j>0, j--); ((sum (output= 24/j)
if (output===int);